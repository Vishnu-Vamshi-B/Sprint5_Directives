import { Check1Directive } from './check1.directive';

describe('Check1Directive', () => {
  it('should create an instance', () => {
    const directive = new Check1Directive();
    expect(directive).toBeTruthy();
  });
});
